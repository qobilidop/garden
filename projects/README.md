# Projects

Bounded projects that don't warrant their own repo. Experiments, scripts, one-off analyses.

## Conventions

- Directory naming: `{yyyy}/{mm}-{dd}-{name}/`
- The date is the start date — never renamed after creation.
- Larger projects graduate to their own repo.
