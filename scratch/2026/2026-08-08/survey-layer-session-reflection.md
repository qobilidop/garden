---
author: Claude Fable 5
---

# Survey layer and methodology-canon session reflection

One session, 2026-08-08 evening: ingested osmani2026-loop-engineering
(post), settled the repo-identity question (stay as-is; machinery over
subject; `sys0` kept), designed the survey layer
(survey-layer-design.md, same directory), ingested the four-work survey
methodology canon with a new wiki page, measured the batch's timing
from the session transcript, and codified the resulting speed rules.

## Applied this session (already committed or in this /evolve pass)

- ingest-paper: OA resolvers (Unpaywall/OpenAlex) before host-probing;
  archiving asynchronous (SPN once, deviation comment as queue);
  co-publication variant (one entry, most-cited DOI primary).
- Global shell discipline (both agent mirrors): `$?` after a pipeline
  is the last command's exit — the tee bug briefly mislabeled a
  capture exit 2 as 0 this session.
- Memory: proactive subagent delegation with model tiering (standing
  feedback); survey-layer summary in sys-personal-system; parked
  HW-spec survey flagged as bootstrap candidate with a venue caveat
  (its own-repo parking predates the hub decision).

## Staged, awaiting recurrence

- **Transcript timing analysis** (first run): parse
  `~/.claude/projects/<project>/<session>.jsonl`, pair `tool_use` ids
  with their `tool_result` timestamps for in-tool durations, treat
  result→next-call gaps as model time, anchor phases on user
  messages. Found: 21.1 min for 4 papers — hunting 28%, SPN polling
  20%, synthesis ~26%, transcription 3%. Promote to a skill if a
  second performance question repeats the procedure.

## Errors worth remembering

- Two zsh aborts violated rules already on the books (unquoted `?`
  glob in a URL; bare `=`-prefixed word) — rule-application failures,
  not rule gaps; no new rule proposed.
- An unverified "65-page" claim survived into a notes draft; the
  skill's self-review caught it, and the two page-count sources
  (Spotlight vs page tree) genuinely disagreed — dropped precision
  instead of picking a side.

## Rejections and non-changes

- Dropping discussions sweeps from posts: rejected by mutual
  agreement — sweeps are posts-only already, cheap there, and twice
  this week supplied the assessment's best evidence (reach numbers,
  the HN comprehension-debt thread).
- No subtraction earned this round; looked at dot-agents-to-try
  (negative-result record, keep) and sys-personal-system's bulk
  (split is a migration without a motivating failure).

## Open threads

- PRISMA capture has no Wayback record yet (SPN triggered twice;
  deviation comment is the queue) — retry on next touch.
- petersen2015 (mapping-guidelines update) closed-access queue item.
- Bootstrap fork: migrate dataflow-survey residue vs run the HW-spec
  survey fresh; the survey skill gets written from whichever goes
  first.
- Zenodo deposition script (pre-reserved DOI, DataCite template from
  dataflow decision 0003); agent-adapted kappa for extraction passes;
  citation-matrix and iteration-decay tooling for screening.
- Unpushed at reflection time: sys 6e0eafd, 9e0161c (+ this pass);
  shadow c441555.
