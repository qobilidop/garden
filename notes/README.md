# Notes

Standalone pieces — my own ideas, written as blog posts. Dated, not rewritten; a newer note supersedes an older one.

## Conventions

- One file per piece: `{yyyy}-{mm}-{dd}-{slug}.md`.
- Stands on its own; external things are springboards, not the subject (those are `comments/`).
