---
import Base from '../layouts/Base.astro'

const base = import.meta.env.BASE_URL.replace(/\/$/, '')
---

<Base title="Not found" noindex>
  <h1>Not found</h1>
  <p>
    No page grows here. Try the <a href={`${base}/`}>front page</a> or
    <a href={`${base}/search/`}>search</a>.
  </p>
</Base>
