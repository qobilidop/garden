= Limitations <sec-limitations>

Terminology is the main discovery limitation: relevant papers may say test
generation or formal simulation, while unrelated papers may adopt the language
of symbolic execution. Multi-index searches, citations, and mechanism review
reduce but cannot remove this risk. 1 strict Semantic Scholar query failed;
the overlapping concepts were searched elsewhere, but unique records may
remain missing.

Full-text inclusion reduces false positives at the cost of false negatives
when old or inaccessible texts cannot be obtained. 59 such or
otherwise unresolved records are parked. The operational boundary is also a
researcher judgment. Each include therefore has an item-specific rationale,
and each close negative names the failed condition, but another defensible
protocol could draw the boundary differently.

Historical search rows recorded canonical new decisions rather than every raw
overlap. Corrective audit rows reconstruct raw positions where retained staging
made that possible. Carter's backward event still resolves only part of its
claimed record set, and the legacy Sylvia event's screened total has no
complete recoverable key partition; neither is treated as complete. Forward
discovery for AutoVeriFix+ and the HLS thesis also remains incomplete because
their citation-index seeds returned no usable records.

Finally, the map codes publications rather than independent tools, retains 14
works only at mapping depth, and does not normalize designs, time bounds,
coverage denominators, harness effort, or completion rules across evaluations.
It therefore supports a qualitative technical synthesis and a reproducible
facet map, not a population estimate, effect-size meta-analysis, or tool
ranking.
