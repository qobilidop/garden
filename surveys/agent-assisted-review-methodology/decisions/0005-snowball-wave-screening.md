# 0005 — Snowball wave: single-pass screening with include-verification

- Status: accepted (agent judgment, 2026-08-08)

The round yielded 1,204 pre-filtered candidates; 881 also match
AI-side vocabulary in title/abstract, 323 do not (conventional
methodology or domain works — excluded en bloc as pre-LLM/background
per the protocol's window rule; sampled before deciding). Dual-pass
screening of 881 exceeds compact scale, so this wave gets ONE haiku
pass; its includes then get a sonnet verification pass. Rationale:
asymmetric error costs — a false exclude loses one catalog row, a
false include is caught by verification (and G1 reading). Deviation
from the all-dual-pass protocol; kappa is reported for wave 1 only.
