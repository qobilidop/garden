# 0003 — Crossref serves peer-review records; protocol lacks a document-type code

- Status: accepted (agent adjudication, 2026-08-08)

Nine screened candidates were peer-review reports and editorial
decision letters (Wiley registers them with DOIs; Crossref returns
them as works). The protocol's exclusion codes are all subject-based;
these fail on document TYPE. Coded E2 with explicit rationale this
campaign. Skill v1 candidate: a dedicated code (e.g. E7
non-study-document) plus a pre-screen filter on Crossref `type`.
