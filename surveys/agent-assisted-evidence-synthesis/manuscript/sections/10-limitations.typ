= Limitations <sec-limitations>

Search coverage is bounded by relevance-sorted top-50 caps, English-only
retrieval, and one failed query in each wave's set. The snowball rounds
apply a title-vocabulary pre-filter, which reintroduces the terminology
dependence snowballing exists to escape @wohlin2014 — and does so
asymmetrically, since the filter reads titles only. This is a recall bound on
every chase, and the record cannot quantify how many in-scope works it cost.
The initial campaign's model-vocabulary list and candidate-level provenance
were not retained, and the later search retains logged queries rather than
unfiltered result sets.

Registrar defects shaped the update materially. Three of twelve
backward chases returned bibliographies the citation index could not
supply, requiring publisher-deposited reference lists; 87 screened
candidates carried no registrar abstract and were judged on title,
venue, and year; 39 rows were coded before-window mechanically from
publication years without a screening pass. Seventy-six records remain
parked as undecidable.

Screening and classification read truncated abstracts (600–900 characters);
every facet was single-pass, with no retained model- or prompt-level audit
trail. The screening passes are repeated automated checks under a human gate,
not independent human review, so correlated classification errors may survive.

The disclosure exemplar we cite is validated by its own
authors, on a stratified sample whose single observed miss carries a
wide interval; the randomized appraisal study we lean on completed 7
of 15 recruited teams.

The integrity review resolved known version aliases across both waves
but cannot prove that no semantically renamed duplicate remains. The
facet labels are not quality assessments. Full-text notes disagree with
abstract-level facets for a minority of evidence notes, including one work
whose coded evidence label describes a comparison its protocol has not
yet performed; the map remains as coded and the notes are authoritative
for those works. Evidence extraction had no second pass. The 31 works were
selected purposively rather than sampled; five notes are abstract-only and one
is secondary-only and supports no finding. This set does not support
literature-wide absence claims — RQ4 in particular had no targeted query or
coding facet.
Medicine accounts for 463 of 776 include-level rows against 23 for
software engineering, so thresholds and examples are
medicine-calibrated, and terminology claims about software engineering
rest on a small deep-read stratum rather than on the map.
